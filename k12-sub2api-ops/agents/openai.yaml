interface:
  display_name: "K12 Sub2API Ops"
  short_description: "Convert, probe, and import K12 OAuth accounts"
  default_prompt: "Use $k12-sub2api-ops to inspect K12 OAuth bundles, probe remaining quota, and choose the safe Sub2API import path."
