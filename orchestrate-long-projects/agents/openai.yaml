interface:
  display_name: "长任务项目编排"
  short_description: "显式维护长期项目台账、共享写入交接和未知副作用对账"
  default_prompt: "使用 $orchestrate-long-projects 时先通过显式路由门和持久连续性门。普通会话中断后读取旧内容继续不使用；只有当前请求属于活动长期项目、需建立长期台账、共享写入交接或未知非幂等动作时才进入。"
policy:
  allow_implicit_invocation: false
